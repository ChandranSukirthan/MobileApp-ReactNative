/**
 * Demo user — must match MongoDB `users._id` and `posts.user` for profile + create post.
 */
export const DEMO_USER_ID = '507f1f77bcf86cd799439011';

export const DEMO_USER = {
  _id: DEMO_USER_ID,
  name: 'Sukirthan',
  email: 'sukirthan@gmail.com',
  bio: 'Arduino enthusiast & maker',
  streak: 7,
};
